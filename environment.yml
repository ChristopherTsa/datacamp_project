name: ramp-battery-prediction
channels:
  - defaults
  - conda-forge
dependencies:
  - joblib
  - matplotlib
  - numpy
  - pandas
  - scikit-learn>=0.22
  - seaborn
  - pip
  - requests
  - pip:
    - ramp-workflow
    - pathlib